<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Multi-company rules: everyone only sees records of companies they are allowed -->
        <record id="rule_contract_template_company" model="ir.rule">
            <field name="name">Contract Template: multi-company</field>
            <field name="model_id" ref="model_contract_template"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('group_contract_legal_user'))]"/>
        </record>

        <record id="rule_contract_contract_company" model="ir.rule">
            <field name="name">Contract: multi-company</field>
            <field name="model_id" ref="model_contract_contract"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('group_contract_legal_user'))]"/>
        </record>

        <!-- Legal User: can only see/manage their own contracts -->
        <record id="rule_contract_contract_user_own" model="ir.rule">
            <field name="name">Contract: Legal User sees own contracts</field>
            <field name="model_id" ref="model_contract_contract"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_contract_legal_user'))]"/>
        </record>

        <!-- Legal Manager / Administrator: unrestricted access -->
        <record id="rule_contract_contract_manager_all" model="ir.rule">
            <field name="name">Contract: Legal Manager sees all contracts</field>
            <field name="model_id" ref="model_contract_contract"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_contract_legal_manager'))]"/>
        </record>

        <!-- Portal users: can only see their own company's contracts -->
        <record id="rule_contract_contract_portal" model="ir.rule">
            <field name="name">Contract: Portal user sees own contracts</field>
            <field name="model_id" ref="model_contract_contract"/>
            <field name="domain_force">[('partner_id', '=', user.partner_id.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        </record>

    </data>
</odoo>
