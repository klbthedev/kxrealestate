<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_contract_article_list" model="ir.ui.view">
            <field name="name">contract.article.list</field>
            <field name="model">contract.article</field>
            <field name="arch" type="xml">
                <list string="Articles" > <!--editable="bottom"-->
                    <field name="sequence" widget="handle"/>
                    <field name="template_id" invisible="context.get('default_template_id')"/>
                    <field name="name"/>
                    <field name="clause_count"/>
                    <field name="active" widget="boolean_toggle"/>
                </list>
            </field>
        </record>

        <record id="view_contract_article_kanban" model="ir.ui.view">
            <field name="name">contract.article.kanban</field>
            <field name="model">contract.article</field>
            <field name="arch" type="xml">
                <kanban class="o_kanban_mobile">
                    <field name="name"/>
                    <field name="clause_count"/>
                    <templates>
                        <t t-name="card">
                            <div class="oe_kanban_global_click p-2">
                                <strong><field name="name"/></strong>
                                <div class="text-muted">
                                    <field name="clause_count"/> clauses
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <record id="view_contract_article_form" model="ir.ui.view">
            <field name="name">contract.article.form</field>
            <field name="model">contract.article</field>
            <field name="arch" type="xml">
                <form string="Article">
                    <sheet>
                        <div class="oe_button_box" name="button_box"/>
                        <group>
                            <field name="template_id" invisible="context.get('default_template_id')"/>
                            <field name="name"/>
                            <field name="sequence"/>
                            <field name="active"/>
                        </group>
                        <field name="description"/>
                        <notebook>
                            <page string="Clauses" name="clauses">
                                <field name="clause_ids">
                                    <list editable="bottom">
                                        <field name="sequence" widget="handle"/>
                                        <field name="name"/>
                                        <field name="mandatory"/>
                                        <field name="active" widget="boolean_toggle"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="view_contract_article_search" model="ir.ui.view">
            <field name="name">contract.article.search</field>
            <field name="model">contract.article</field>
            <field name="arch" type="xml">
                <search string="Search Articles">
                    <field name="name"/>
                    <field name="template_id"/>
                    <filter string="Template" name="group_template" context="{'group_by': 'template_id'}"/>
                </search>
            </field>
        </record>

        <record id="action_contract_article" model="ir.actions.act_window">
            <field name="name">Articles</field>
            <field name="res_model">contract.article</field>
            <field name="view_mode">list,kanban,form</field>
        </record>
        <menuitem id="menu_contract_article_list" name="Articles"
                  parent="menu_contract_header" action="action_contract_article" sequence="20"/>

    </data>
</odoo>
